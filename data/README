Data for building Cytoskaler (testing, training, validation image sets) can be found in /net_skel and /net_plsm, 
which correspond to image sets for the two different networks that were trained. 
CSV files indicate which images were used in the training, testing, and validation sets, 
and the labels asscociated with each image.
 
The DAG networks are in the form of .mat files and can be found in /networks folder.

Images used for analysis can be found in the /analysis folder.



